1 Get Boost
1.0 Download boost_1_61_0.tar.bz2
1.1 tar --bzip2 -xf /path/to/boost_1_61_0.tar.bz2

2 Build and Install
some are Header-Only Libraries,some are  must be built separately 
A few libraries have optional separately-compiled binaries

cd path/to/boost_1_61_0
./bootstrap.sh --help
./bootstrap.sh --prefix=path/to/installation/prefix
./b2 install
